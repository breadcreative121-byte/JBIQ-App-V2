// Re-export the auto-generated JDS tokens so RN code imports from a stable local
// path (`@theme`). Never edit tokens here — edit tokens/jds.tokens.json and run
// `npm run tokens` at the repo root to regenerate tokens/tokens.ts.
export * from '../../../../tokens/tokens';
export { tokens as default } from '../../../../tokens/tokens';
